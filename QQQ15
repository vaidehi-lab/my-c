#include<stdio.h>
void main()
{
int num,orignum,digit,sum=0;//check armstrong number. 
clrscr();
printf("ENTER ANY NUMBER:");
scanf("%d",&num);
orignum=num;
while(num>0)
{
digit=num%10
sum=sum+digit*digit*digit;
num=num/10;
}
if(sum==orignum)
  printf("The number entered is an Armstrong Number.");
else
  printf("The number entered is not an Armstrong Number.");
getch();
}
