#include<stdio.h>
void main()
{
int num;
clrscr();
printf("ENTER A NUMBER:");//finding the absolute value of a number.
scanf("%d",&num);
if(num<0)
  printf("The absolute value of %d is %d",a,-a);
else
  printf("The absolute value of %d is %d",a,a);
getch();

}
