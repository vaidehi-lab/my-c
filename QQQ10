#include <stdio.h>
void main()
{
int num, revNum = 0, remainder; // find reverse of a number.
printf("Enter a number: ");
scanf("%d", &num);
while (num != 0) 
{
remainder = num % 10;
revNum = revNum * 10 + remainder;
num =num/10;
}
printf("Reversed Number = %d\n", revNum);
getch();
}
