#include <stdio.h>
void main() 
{
int num;
int count = 0; // count the number of digits.
printf("Enter a number: ");
scanf("%d", &num);
if (num == 0) 
    count = 1;
else{
    int temp= num; 
    while (temp!= 0) 
    {
          temp=temp/10; 
          count++;           
    }
    }
printf("Number of digits in %d is %d\n", num, count);
getch();
}
